//segmentation fault, core dumped
class Main
{
    static void run()
    {
        ifj16.print("Zadaje slovo: ");
        String h = ifj16.readString();
        h = ifj16.sort(h);

        String juntos = h+h;

        juntos = ifj16.sort(juntos);

        ifj16.print("sorted: " +juntos);

        String vysledok = ifj16.substr(juntos, 2, 4);
        ifj16.print("Vysledok ? " + vysledok);


    }
}
