#ifndef BUILTIN_H
#define BUILTIN_H

int length (char *string);
int compare (char *s1, char *s2);
char * substr (char *s, int i, int n);
int read_int();
double read_double();
char * read_string();

#endif // BUILTIN_H
