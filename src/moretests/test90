//number of digits

class Main {
	static void run () {
		int a = 0;
		int no = 87654;

		if (no < 0) {
			no = no * (0-1);
		}

		else if (no == 0) {
			no = 1;
		}

		while  (no > 0) {
			no = no/10;
			a = a+1;
		}

		ifj16.print("Number of digits is: " +a+ "\n");
	}
}
